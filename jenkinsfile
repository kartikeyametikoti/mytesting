pipeline {
    agent any

    tools {
        maven 'Maveni 3.9.7' // Use the name specified in Maven installation configuration
    }

    stages {
        stage('Build') {
            steps {
                sh 'mvn clean install' // Use Maven command in your pipeline steps
            }
        }

        // Add more stages as needed
    }
}
